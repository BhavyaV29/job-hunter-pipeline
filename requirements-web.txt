# Runtime deps for the web app (server.py) + the pipeline it drives.
# Playwright is intentionally excluded to keep the image small; the Naukri
# Playwright route is disabled in the container via NAUKRI_SKIP_PLAYWRIGHT=1
# (keyless/keyed aggregator APIs still fill the tracker).
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
apscheduler>=3.10

# pipeline core
httpx>=0.27
requests>=2.31
pyyaml>=6.0
beautifulsoup4>=4.12
gspread>=5.12
google-auth>=2.29
